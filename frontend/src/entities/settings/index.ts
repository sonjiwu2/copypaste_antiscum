export type { AppSettings, AvatarId, SettingsState, Theme } from './model/types'
export {
  AVATAR_ORDER,
  DEFAULT_SETTINGS,
  avatarUrl,
  defaultSettingsForProfile,
  effectsGain,
  musicGain,
  nextAvatar,
  useSettingsStore,
} from './model/settingsStore'
